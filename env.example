# Admin dashboard credentials
# Copy this file to .env.local and set your values

# Password required to log into /admin
ADMIN_PASSWORD=changeme

# Secret used to sign the session cookie — change this to a random string
ADMIN_SECRET=change-this-to-a-random-secret
